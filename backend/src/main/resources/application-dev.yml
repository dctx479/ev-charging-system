spring:
  # 开发环境配置
  datasource:
    url: jdbc:mysql://localhost:3306/ev_charging_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root123456

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# 开发环境日志级别
logging:
  level:
    com.ev.charging: debug
